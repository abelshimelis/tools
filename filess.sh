#!bin/bash
#SIMPLE WEB PENTESTER INSTALLER

#Scanning ports and vulnurabilities
sudo apt install nmap -y

#best expoloitation tool
sudo apt install msf console

#burpsuite
sudo apt install burp suite

#Vulnurability scanner
sudo apt install nikto

#command line to upgarde & update tools
sudo apt-get update && upgrade

#performing brute force to the dir
sudo apt install gobuster

#ip address scanner 
sudo apt install nslookup

#power full vunerablity scanner
sudo dpkg -i Nessus-10.5.4-ubuntu1404_amd64.deb 

#to perform reverse shell
sudo apt install netcut

done
